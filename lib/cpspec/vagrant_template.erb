Vagrant::Config.run do |config|
  config.vm.define "<%= cookbook_name %>" do |conf|
    conf.vm.box = "<%= platform%>"
    conf.vm.provision :chef_solo do |chef|
      chef.cookbooks_path = "<%= cookbook_path%>"
      chef.add_recipe "<%= recipe %>"
    end
  end
end
